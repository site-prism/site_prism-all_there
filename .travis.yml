language: ruby
sudo: required
dist: xenial

rvm:
  - 2.4
  - 2.5
  - 2.6
  - ruby-head

gemfile:
  - Gemfile

before_install:
  - gem uninstall bundler
  - gem install bundler -v '< 2'

script:
  - bundle exec rake

branches:
  only:
    - master

matrix:
  allow_failures:
    - rvm: ruby-head
